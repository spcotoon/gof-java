package study.java.gof.pojo._01_creational_patterns.p05_prototype.ex02_after;

public class A_Readme {
    /**
     * 복잡한 객체를 만드는 과정을 숨길 수 있다.
     * 기존 객체를 복제하는 과정이 새 인스턴스를 만드는 것 보다
     * 비용(시간 또는 메모리)적인 면에서 효율적일 수 있다.
     * 추상적인 타입을 리턴할 수 있다.
     *
     * 단점으론, 복제한 객체(순환 참조가 있는 경우 등)를 만드는 과정이 복잡할 수 있다.
     */
}
